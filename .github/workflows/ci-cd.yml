name: ci-cd-backend
on:
  push:
    branches: [ main ]
  workflow_dispatch:

concurrency:
  group: wedding-backend-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test-and-lint:
    runs-on: self-hosted
    defaults:
      run:
        shell: bash
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          clean: true

      # ---- CI (validation & tests) ----
      - name: Install dependencies
        run: |
          python3 -m pip install --upgrade pip
          python3 -m pip install -r requirements.txt

      - name: Validate FastAPI app starts
        env:
          SECRET_KEY: test_secret_key_for_ci
          GOOGLE_CLIENT_ID: test_google_client_id
        run: |
          timeout 10 python3 -m uvicorn main:app --host 127.0.0.1 --port 8001 &
          sleep 5
          curl -f http://127.0.0.1:8001/guest || exit 1
          pkill -f "uvicorn main:app" || true

  build-and-deploy:
    runs-on: self-hosted
    needs: test-and-lint
    defaults:
      run:
        shell: bash
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          clean: true

      # ---- Sync working tree to deployment dir ----
      - name: Sync to /srv/wedding/backend
        run: |
          rsync -a --delete --exclude='.git' ./ /srv/wedding/backend/

      # ---- Build & Deploy with Compose on the server ----
      - name: Build backend image with BuildKit
        env:
          DOCKER_BUILDKIT: 1
          COMPOSE_DOCKER_CLI_BUILD: 1
        run: docker compose -f /srv/wedding/docker-compose.yml build backend

      - name: Start/Update backend
        run: docker compose -f /srv/wedding/docker-compose.yml up -d --no-deps backend

      - name: Cleanup dangling images
        run: docker image prune -f
